'use strict';

describe('Service: IfExistsElse', function () {

  // load the service's module
  beforeEach(module('bulbsCmsApp'));

  // instantiate service
  var IfExistsElse;
  beforeEach(inject(function (_IfExistsElse_) {
    IfExistsElse = _IfExistsElse_;
  }));

  /*it('should do something', function () {
    expect(!!IfExistsElse).to.equal(true);
  });*/

});
